- Dependencies

# Nodemon = is a librarby that refreshes the server each time we make some changes and save them, it is supposed to refresh the server automatically.

# Cors = it creates a connection between front-end and back-end server so that there are no issues making these API calls.